# Results

- orm-benchmark (with no flags)

```
   @(with-no-flag)
```

- orm-benchmark -multi=5

```
  @(multi-5)
```

- orm-benchmark -multi=10

```
  @(multi-10)
```

- orm-benchmark -multi=20

```
  @(multi-20)
```
